extends layout

block content
  h1= title
  p Welcome to #{title}

  form(action="/upload",method="POST",enctype="multipart/form-data")
    input(type="file",name="file")
    button(type="submit") 送信
    div#message= _message
